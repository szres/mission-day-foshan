node_modules/
.wrangler/
.dev.vars
*.p8
.DS_Store
.claude/

# MapKit JS token — bound to your own domain, never commit
public/mapkit-token.js

# Large source assets / intermediate files
headimg.png

# Generated planning artefacts (not deployed; not part of the published site)
checklist.csv
checklist.md

# Third-party PDFs (e.g. the organisers' official mission-overview booklet).
# Stays on disk for local use / private sharing; never published or committed.
*.pdf
